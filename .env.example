# Supabase (recommended): automatic live publish. Every save in Admin updates the live site.
# Get URL and anon key from Supabase Dashboard → Settings → API.
# VITE_SUPABASE_URL=https://xxxx.supabase.co
# VITE_SUPABASE_ANON_KEY=your_anon_public_key

# Optional: static portfolio URL if not using Supabase.
# VITE_PORTFOLIO_URL=https://sphnsx.com/portfolio.json
